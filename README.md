## Virtual Machine calculate by first fit strategy

After clone the object run commands below:

```
# to install dependencies
composer install

# run tests
./vendor/bin/phpunit

# generate phpmetric reports for consult
./vendor/bin/phpmetrics --report-html=report 
```
